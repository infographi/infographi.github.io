---
import "../../styles/global.css";
import { EMAIL_ADDRESS, SITE_TITLE, SITE_DESCRIPTION, TELEPHONE_NUMBER } from "../../consts";
import { getRelativeLocaleUrl } from "astro:i18n";
import BaseHead from "../../components/BaseHead.astro";
import Link from "../../components/Link.astro";
import SkipContent from "../../components/SkipContent.astro";

const homeURL = getRelativeLocaleUrl("it", "it");
---

<!doctype html>
<html lang="en">
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
    <body id="nothing-page">
        <SkipContent />
        <main id="content">
            <section>
                <h2>404</h2>

                <dfn>Ops, qualcosa è andato storto.</dfn>

                <p>La pagina a cui si sta tentando di accedere non esiste. Questa pagina non contiene nulla di utile. <span id="does-it" class="glitch" data-text="O forse?" style="visibility: hidden;">O forse?</span></p>

                <p>Segui il link per tornare al sito:  <Link id="home-link" href={homeURL}>home</Link> </p>

                <p id="bon">Felicitazioni!!! Hai appena vinto un audit gratuito per un tuo dispositivo scelta. <Link  href={TELEPHONE_NUMBER}>Chiamateci</Link> o <Link href={EMAIL_ADDRESS}>Inviateci un'email</Link> e richiedete il vostro premio! La password da comunicare è: <span class="pass">"MACHINE"</span>.</p>
            </section>
        </main>
    </body>
</html>

<style>

    #nothing-page,
    #bon {
        display: block;
        outline-color: transparent;
        outline-offset: calc(var(--mesure-small) * -1);
        outline-width: var(--mesure-small);
        padding: var(--mesure-small);
    }

    main {
        max-inline-size: 100ch;
    }

    h2 {
        font-size: var(--mesure-xxxx-large);
    }

    h2:before {
        content: "#";
    }

    section {
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
    }

    section>*+* {
        margin-block-start: var(--mesure-medium);
    }

    #bon {
        color: var(--color-dark-void);
    }

    #bon:hover {
        animation-duration: 0.5s;
        animation-timing-function: ease-in-out;
        animation-fill-mode: forwards;
        animation-name: Stay-Visible;
        border: var(--mesure-xxx-small) dashed var(--accent-color-shade);
        border-radius: 1ch;
    }

    #home-link {
        color: var(--accent-color);
    }

    #home-link:hover,
    #home-link:focus {
        color: var(--color) !important;
    }

    @keyframes Stay-Visible {
        from {
            color: var(--color-void);
        }
        to {
            color: var(--color-color);
        }
    }

    #pass {
        font-weight: 900 !important;
    }
</style>